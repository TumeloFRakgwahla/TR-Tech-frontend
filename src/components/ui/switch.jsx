/**
 * Switch Component (Re-export)
 *
 * This file re-exports the Switch component from the
 * parent components directory. It serves as a convenience alias so that
 * consumers can import from either `components/ui/switch` or `components/switch`.
 *
 * The Switch component is a toggle control used for boolean settings like
 * enabling/disabling features or preferences.
 *
 * Re-exports:
 *   - Switch: The switch UI component
 */

// eslint-disable-next-line react-refresh/only-export-components
export { Switch } from '../switch';
